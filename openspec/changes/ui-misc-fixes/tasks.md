## 1. Fix 1 — Dashboard download section positioning

- [ ] 1.1 Commit the stashed change to `Dashboard.vue` that removes the `centered` class, `hasStarted` ref, and associated CSS

## 2. Future Fixes

- [ ] 2.1 (placeholder) Discover, fix, and commit next UI issue found during testing — update spec with new requirement before closing this task
